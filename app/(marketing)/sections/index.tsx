/**
 * Home sections — STUB.
 *
 * W2 (ENG-588) replaces this file wholesale with the twelve ported content
 * sections: hero, ribbon, what-is, how-it-works, the app, members, pricing,
 * why, trainers carousel, FAQ, for-trainers and the join banner. Every class
 * those sections need is already in app/(marketing)/marketing.css, which is a
 * complete port of the mockup's stylesheet — W2 adds markup only and must not
 * need to touch the stylesheet.
 *
 * `id="top"` lives here rather than on the nav so the wordmark and the footer's
 * "Home" anchor resolve in the W1 shell. W2's hero carries the same id when it
 * takes this file over, matching the mockup's <header class="hero" id="top">.
 */
export default function HomeSections() {
  return <main id="top" />;
}
